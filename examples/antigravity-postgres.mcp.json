{
  "mcpServers": {
    "mcp-hub-postgres": {
      "command": "npx",
      "args": ["-y", "mcp-hub", "stdio", "postgres"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
