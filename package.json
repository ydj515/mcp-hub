{
  "name": "mcp-hub-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/servers/*",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc -b packages/core packages/servers/shortcuts packages/servers/api-finder packages/servers/postgres packages/cli",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.1.3",
    "tsx": "^4.9.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
